name: Build Android APK (For Mid-term)
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          submodules: recursive # 自动拉取子模块

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install NativeScript
        run: npm install -g nativescript@8.6.0 --ignore-scripts

      - name: Install Dependencies
        run: |
          npm install --legacy-peer-deps
          npx ts-patch install -s

      - name: Install Android SDK 33
        run: |
          # 【关键】把路径写入 GitHub 的全局变量文件，让它对后面的步骤也生效
          echo "$ANDROID_HOME/cmdline-tools/latest/bin" >> $GITHUB_PATH
          echo "$ANDROID_HOME/platform-tools" >> $GITHUB_PATH
          
          # 临时刷新当前步骤的 PATH，以便马上能用
          export PATH=$ANDROID_HOME/cmdline-tools/latest/bin:$PATH
          
          yes | sdkmanager --licenses
          sdkmanager "platforms;android-33" "build-tools;33.0.0"

      - name: Build APK
        run: |
          # --force 参数强制忽略“缺少模拟器”的警告，强行开始编译
          ns build android --release --no-hmr --env.abiFilters=arm64-v8a --force

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          # 注意：NativeScript 默认输出路径可能包含 release 或 debug，这里通配符匹配
          path: platforms/android/app/build/outputs/apk/release/*.apk
